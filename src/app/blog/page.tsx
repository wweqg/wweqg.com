export default function BlogPage() {
  return (
    <div
      className="mx-auto max-w-[720px]"
      style={{ padding: '160px var(--s-7) var(--s-9)' }}
    >
      <h1
        className="font-bold"
        style={{
          fontSize: 'var(--t-3xl)',
          letterSpacing: '-1.5px',
          marginBottom: 'var(--s-5)',
        }}
      >
        Blog
      </h1>
      <p style={{ color: 'var(--text-2)', fontSize: 'var(--t-base)' }}>
        Coming soon.
      </p>
    </div>
  )
}
